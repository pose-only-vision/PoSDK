# 1DSfM 数据集

（v2版本后续完善）
<!--
1DSfM (1-Day Structure from Motion) 数据集通常包含大规模的无序图像集合，是评估大规模重建和定位算法的常用基准。

本页将讨论（预留）：

- **数据格式**: `bundler.out` 文件格式，图像列表，特征和匹配信息。
- **PoSDK中的加载与转换**: 如何解析1DSfM数据集的输出，将其转换为PoSDK的`GlobalPoses`, `Tracks`, `CameraModels`等数据结构。
- **评估方法**: 在大规模场景下评估全局位姿精度的方法。

(内容待填充) 
-->